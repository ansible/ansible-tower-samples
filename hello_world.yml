- name: Hello World Sample
  hosts: all
  # gather_facts: false
  connection: ssh
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!"

    - name: Refresh DNSMASQ Service
      systemd:
        state: restarted
        name: dnsmasq
      become: yes
      remote_user: ansibleadm
