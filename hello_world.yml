- name: Hello World Sample
  hosts: all
  # gather_facts: false
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!"

    - name: Refresh DNSMASQ Service
      systemd:
        state: restarted
        name: dnsmasq
      become_user: root
      become: yes
      become_method: sudo
      remote_user: ansibleadm
