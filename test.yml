---
  
- hosts: all
  become: yes
  become_method: sudo
  gather_facts: no

  tasks:
    - name: Place file on server
      ansible.builtin.copy:
        src: /tmp/dummyFile
        dest: /tmp/dummyFile
        owner: root
        group: root
        mode: 0600

    - name: Add a line to a file if the file does not exist, without passing regexp
      ansible.builtin.lineinfile:
        path: /tmp/dummyFile
        line: Testing Adding A Line
        create: yes 

    
